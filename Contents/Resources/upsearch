# upsearch() looks up the path until it finds the target directory

upsearch () {
    local start_path=$1
    local go_path=$start_path
    while [[ "$go_path" != "" && ! -e "$go_path/$2" ]]; do
        go_path=${go_path%/*}
    done
    echo "$go_path"
}
